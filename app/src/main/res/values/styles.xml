<resources>


  <!-- Base application theme. -->
  <style name="iOSTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="colorPrimary">@color/primary</item>
    <item name="colorPrimaryDark">@color/primary_dark</item>
    <item name="colorAccent">@color/accent</item>
    <item name="android:textColor">@color/primary_text</item>
    <item name="android:textColorSecondary">@color/secondary_text</item>
    <item name="android:textColorPrimary">@color/primary</item>
    <item name="android:icon">@color/icons</item>
    <item name="android:divider">@color/divider</item>
  </style>


  <!-- Base application theme. -->
  <style name="ColdStartTheme" parent="iOSTheme">
    <item name="android:windowBackground">@drawable/cold_start_bg</item>
  </style>


  <!-- Base application theme. -->
  <style name="MainTheme" parent="ColdStartTheme">

  </style>

  <style name="DetailedTheme" parent="MainTheme">
    <!--背景透明-->
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <!--无进入动画-->
    <item name="android:windowAnimationStyle">@null</item>
  </style>


  <style name="Settings" parent="MainTheme">
    <item name="android:windowAnimationStyle">@android:anim/slide_in_left</item>

  </style>


</resources>
